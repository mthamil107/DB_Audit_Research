# RESULTS — Self-Defeating Audits — MariaDB 10.11 (MySQL family)

_Local, disposable, synthetic lab. Generated by `run_matrix_mysql.sh 10.11`._

## Environment

| Field | Value |
|---|---|
| Image | `mariadb:10.11` |
| Image digest | `mariadb@sha256:8020e05c4c498d06c87f0a1db010eb79bd6f8fb30e9b763d4690c34ce1e61008` |
| `VERSION()` | 10.11.18-MariaDB-ubu2204-log |
| Binary log | enabled (`--log-bin`); `log_bin_trust_function_creators=1` |

> Victim triggers are **DEFINER=`root`** (run with the definer's privileges — the MySQL
> analogue of SECURITY DEFINER), so a low-privilege writer can be audited. MySQL has **no
> `search_path`**, so the PostgreSQL 1A/1B catalog-shadow class does not transfer.

## Feasibility matrix

| Vector | Precondition | Result | Reversible / residue | True privilege needed |
|---|---|---|---|---|
| FN function/catalog shadow | app tries to shadow the capture built-in | **N/A** — no search_path; built-in JSON_OBJECT is not shadowable and app lacks CREATE ROUTINE (planted routines=0) → write still audited (logged=1) | — | the entire PG 1A/1B class does not transfer |
| 1C replace trigger | app has TRIGGER priv on the table | **BLINDED** (sentinel logged=0, ctl logged=0) | restore to root-DEFINER FAILED (app cannot set DEFINER=root); AFTER INSERT root-definer trigger present now=0 → residue remains | TRIGGER privilege on the table |
| 2 GUC/session dormant | TRIGGER priv AND audit-INSERT grant | **DORMANT-BYPASS WORKS (gated skipped, ctl logged)** (gated logged=0 / ctl logged=1) | at rest looks legit; **needs the extra audit-INSERT grant** (harder than PostgreSQL) | TRIGGER priv + INSERT on audit table |
| 3 attribution poison | broad INSERT grant on audit table | **POISONED (fabricated identity accepted)** (ghost=1, backdated=1) | **NO** — schema-identical to genuine rows; needs cross-layer correlation | INSERT on audit table |

**1C trigger snapshot (object diff):**
```
before: accounts_ad:root@localhost | accounts_ai:root@localhost | accounts_au:root@localhost
after : accounts_ad:root@localhost | accounts_au:root@localhost
```

## Defenses

| Defense | Result |
|---|---|
| Remove TRIGGER privilege from app account | app_user without TRIGGER priv cannot DROP/replace the audit trigger → write still logged (logged=1) |
| Separate DEFINER (app cannot set DEFINER=root) | app-created triggers cannot impersonate root; the DEFINER change is an un-erasable residue (see 1C) |
| Engine log (general_log; pgaudit/binlog analogue) | general query log CAPTURED the blinded write (matches=2) in an engine log app_user cannot read or purge |
| Hash-chained append-only sink | intact=NULL; tampered=4 (non-NULL seq = tamper detected) |
| `log_bin_trust_function_creators=0` + binlog | incidental barrier — blocks non-SUPER trigger creation entirely (not exercised here; set to 1 to isolate the TRIGGER-privilege variable) |

_Evidence: `results/_evidence_mariadb_10.11.log`._
